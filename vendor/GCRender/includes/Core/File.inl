#include "File.h"
/*
template <typename T>
int File::Read(T& value, uint32 count)
{
	//char8* buffer[];
	//char8 current;

	return fread(&value, sizeof(T), count, m_content);

	//for (int i = 0; i < length; i++)
	//{
	//	current = ReadByte();
	//
	//	// If an element of the length isn't a binary
	//	if (current != '0' && current != '1') return;
	//	buffer[i] = current;
	//}
	//value = buffer;
}
*/